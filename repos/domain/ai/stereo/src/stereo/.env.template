# Stereo module — AI approval/rejection
# Inherits from app root .env; these are the vars this module uses.
NATS_URL=
NATS_QUEUE_GROUP=stereo-workers
MONGO_URI=
MONGO_DB_NAME=stereo
OPENAI_API_KEY=
AI_MODEL=gpt-4o-mini
